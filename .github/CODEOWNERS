# CODEOWNERS — stroupaloop/mission-control (private fork)
#
# All AMS extension code requires owner review. Upstream-sensitive files
# (db.ts, layout.tsx) also flagged so any change is visible.

# Extensions — all custom code lives here
/src/extensions/            @stroupaloop

# Upstream touch-points — tightly controlled
/src/lib/db.ts              @stroupaloop
/src/app/layout.tsx         @stroupaloop

# Fork policy + workflows
/FORK.md                    @stroupaloop
/.github/                   @stroupaloop
